{
	"name": "cmdlook",
	"version": "1.0.0",
	"description": "Watch a directory and run commands when files change.",
	"keywords": [
		"cli",
		"watch",
		"command",
		"file-watcher"
	],
	"license": "MIT",
	"main": "index.js",
	"bin": {
		"cmdlook": "./index.js"
	},
	"scripts": {
		"format": "bunx prettier . --write",
		"release": "yarn version",
		"prerelease": "# bun install && bun format && bun run test",
		"postrelease": "npm publish && git push && git push --tags && echo \"Successfully released version $npm_package_version\""
	},
	"prettier": {
		"useTabs": true,
		"printWidth": 100,
		"singleQuote": true,
		"arrowParens": "avoid",
		"trailingComma": "es5",
		"bracketSpacing": false
	}
}
